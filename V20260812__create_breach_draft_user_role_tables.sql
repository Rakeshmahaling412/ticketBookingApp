CREATE TABLE breach_drafts (
id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
batch_id VARCHAR(255),
compliance_officer VARCHAR(255) NOT NULL,
 email_address VARCHAR(255) NOT NULL,
employee_name VARCHAR(255),
employee_igg VARCHAR(255),
business_unit VARCHAR(255),
legal_entity VARCHAR(255),
location VARCHAR(255),
manager_email_address VARCHAR(255),
transversal_breach VARCHAR(255),
policy VARCHAR(255),
breach_type VARCHAR(255),
description CLOB,
identified_breach_date DATE,
breach_dates ARRAY,
status VARCHAR(255),
identification_method VARCHAR(255),
root_cause VARCHAR(255),
licensed_staff VARCHAR(255),
regulators_notified VARCHAR(255),
email_status VARCHAR(255),
email_content CLOB,
email_comments_to_staff CLOB,
comments CLOB,
reported_to_regulator_date DATE,
action_plan_id VARCHAR(255),
action_plan_short_description VARCHAR(255),
action_plan_due_date DATE,
action_plan_completed_date DATE,
committee_name VARCHAR(255),
date_sent_to_committee DATE,
sme_confirmed_severity INTEGER,
breach_score INTEGER,
occurrence INTEGER,
cumulative_breach_score INTEGER,
subtype_qa_list ARRAY,
job_title VARCHAR(255),
contract_type VARCHAR(255),
physical_start_date DATE,
local_department VARCHAR(255) NOT NULL,
entity VARCHAR(255),
VARCHAR(255),
action VARCHAR(255),
t2eorem VARCHAR(255),
email_sent_timestamp TIMESTAMP,
email_sent_to VARCHAR(255),
email_cc VARCHAR(255),
recommended_action VARCHAR(255),
create_by VARCHAR(255) NOT NULL,
create_time TIMESTAMP NOT NULL,
last_update_by VARCHAR(255) NOT NULL,
last_update_time TIMESTAMP NOT NULL);

CREATE TABLE user (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                   active BOOLEAN,
                   profiles VARCHAR(255),
                   frozen_roles BOOLEAN,
                  department VARCHAR(255),
                  country VARCHAR(255),
                  first_name VARCHAR(255) NOT NULL,
                  last_name VARCHAR(255) NOT NULL,
                  igg VARCHAR(255) NOT NULL,
                  email VARCHAR(255) UNIQUE,
                  login_count INTEGER);

CREATE TABLE role (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 name VARCHAR(255) UNIQUE);

CREATE TABLE roles_users (user_id BIGINT NOT NULL,
                             role_id BIGINT NOT NULL,
                             PRIMARY KEY (user_id, role_id),
                             FOREIGN KEY (user_id) REFERENCES user(id),
                             FOREIGN KEY (role_id) REFERENCES role(id));
